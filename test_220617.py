def function (alpha, beta=True):
    """A function

    Args:
        alpha (int): ddd
        beta (bool, optional): aaa. Defaults to True.

    Returns:
        bool : a return value
    """
    if alpha == -1 :
        print("negative!")
    return beta
